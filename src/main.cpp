#include "CSRMatrix/CSRMatrix.hpp"
#include <iostream>

int main() {
    // std::vector<std::vector<double>> A = {{1, 2}, {3, 4}};
    // std::vector<double> x = {1, 1};
    // std::vector<double> result = multiply_matrix_vector(A, x);

    // for (double val : result)
    //     std::cout << val << " ";
    // std::cout << std::endl;
    return 0;
}
